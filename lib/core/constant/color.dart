
import 'package:flutter/material.dart';

class AppColor{
  static const Color primary = Color(0xFF2D8B9D);
  
  // Secondary colors
  static const Color secondary = Color(0xFF34A7B8);  // Lighter teal for highlights
  static const Color accent = Color(0xFF1B5E6D);     // Darker teal for depth
  
  // Status colors
  static const Color successColor = Color(0xFF4CAF50);  // Semantic green for success
  static const Color errorColor = Color(0xFFE53935);    // Semantic red for errors
  static const Color warningColor = Color(0xFFFFA726);  // Orange for warnings
  
  // Neutral colors
  static const Color background = Color(0xFFFAFAFA);    // Light grey background
  static const Color surface = Color(0xFFFFFFFF);       // Pure white for cards
  static const Color grey = Color(0xFF757575);          // Medium grey for secondary text
  static const Color darkGrey = Color(0xFF424242);      // Dark grey for primary text
  static const Color lightGrey = Color(0xFFE0E0E0);     // Light grey for borders
  
  // Text colors
  static const Color textPrimary = Color(0xFF212121);   // Near black for primary text
  static const Color textSecondary = Color(0xFF757575); // Grey for secondary text
  static const Color textLight = Color(0xFFFFFFFF);  
}